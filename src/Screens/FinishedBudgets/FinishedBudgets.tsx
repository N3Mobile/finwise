import React from "react";

export const FinishedBudgets = () => {

    // useFocusEffect(
    //     useCallback(() => {
    //         http.get('budgets/ranges', {
    //             wallet_id: 
    //             start_date: 
    //             end_date: 
    //         }).then(data => {
    //             console.log(data);

    //         }).catch(error => setError(error.toString()));
    //     }, [])
    // );

    // TODO
    return (
        <>
        </>
    )
}